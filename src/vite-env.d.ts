/* Vite's ambient types. Teaches TypeScript about the non-JS imports the app
   uses: plain `.css` side-effect imports, and `.module.css` files resolving to
   an object of class names. */

/// <reference types="vite/client" />
